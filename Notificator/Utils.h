#ifndef UTILS_H
#define UTILS_H

#include <string>

using namespace std;

class Utils {
public:
    static string intToString(int i);
    static string longToString(long l);
private:

    Utils() {
    };
};

#endif
