<?xml version="1.0" encoding="UTF-8"?>
<tasks>
	<task id="1" version="1"> <!-- id and version should always be integer. -->
        <name>AliveCheck</name>
        <!-- Task must be of types: timed, trigger, combined. -->
        <type>timed</type>
        <!-- Path relative to binary. -->
        <path>./tasks/AliveCheck/AliveCheck.so</path>
    </task>
    <task id="2" version="1"> <!-- id and version should always be integer. -->
        <name>Watchdog</name>
        <!-- Task must be of types: timed, trigger, combined. -->
        <type>trigger</type>
        <!-- Path relative to binary. -->
        <path>./tasks/Watchdog/Watchdog.so</path>
    </task>
</tasks>
